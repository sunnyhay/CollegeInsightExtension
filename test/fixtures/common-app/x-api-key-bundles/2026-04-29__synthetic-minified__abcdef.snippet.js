// Synthetic fixture for the unquoted-key, equals-assigned shape sometimes
// produced by Angular minifier output (matches the second EXTRACTION_PATTERN).
// See README.md in this directory for capture procedure.
var c = (e.HEADERS = { Authorization: t, "X-APi-Key": "abcdefghijklmnopqrstuvwxyz012345" });
